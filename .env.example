PORT=9999

# MongoDB Credentials
MONGO_ROOT_USERNAME=your_root_username
MONGO_ROOT_PASSWORD=your_secure_password
MONGO_DATABASE=medium-clone

# MongoDB URI Configuration
# Option 1: Local development (uncomment this line if running MongoDB locally)
MONGODB_URI=mongodb://localhost:27017/medium-clone

# Option 2: Docker with authentication (uncomment this line if using Docker)
# MONGODB_URI=mongodb://${MONGO_ROOT_USERNAME}:${MONGO_ROOT_PASSWORD}@medium-clone-mongodb:27017/${MONGO_DATABASE}?authSource=admin

JWT_SECRET=your-jwt-secret-key

# Mongo Express Credentials
ME_CONFIG_BASICAUTH_USERNAME=your_admin_username
ME_CONFIG_BASICAUTH_PASSWORD=your_admin_password
